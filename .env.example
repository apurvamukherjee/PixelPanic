# Copy to server/.env for local overrides (optional — sensible defaults apply).
# Not auto-loaded; only used if you wire in a dotenv loader or export these
# vars in your shell / deploy platform (Fly.io/Render env settings).
PORT=3001
CORS_ORIGIN=http://localhost:5173
DB_PATH=data/pixelpanic.sqlite
